class graphite::sumo {
    # file{
    #     "/etc/sumo.d/graphite_app_clients.json":
    #         ensure  => file,
    #         replace => 'true',
    #         owner   => 'root',
    #         group   => 'root',
    #         mode    => '0644',
    #         content => template("sumo/graphite_app_clients.json.erb");
            
    #     "/etc/sumo.d/graphite_app_console.json":
    #         ensure  => file,
    #         replace => 'true',
    #         owner   => 'root',
    #         group   => 'root',
    #         mode    => '0644',
    #         content => template("sumo/graphite_app_console.json.erb");
            
    #     "/etc/sumo.d/graphite_app_creates.json":
    #         ensure  => file,
    #         replace => 'true',
    #         owner   => 'root',
    #         group   => 'root',
    #         mode    => '0644',
    #         content => template("sumo/graphite_app_creates.json.erb");

    #     "/etc/sumo.d/graphite_app_listener.json":
    #         ensure  => file,
    #         replace => 'true',
    #         owner   => 'root',
    #         group   => 'root',
    #         mode    => '0644',
    #         content => template("sumo/graphite_app_listener.json.erb");

    #     "/etc/sumo.d/graphite_app_query.json":
    #         ensure  => file,
    #         replace => 'true',
    #         owner   => 'root',
    #         group   => 'root',
    #         mode    => '0644',
    #         content => template("sumo/graphite_app_query.json.erb");

    #     "/etc/sumo.d/graphite_nginx_access.json":
    #         ensure  => file,
    #         replace => 'true',
    #         owner   => 'root',
    #         group   => 'root',
    #         mode    => '0644',
    #         content => template("sumo/graphite_nginx_access.json.erb");

    #     "/etc/sumo.d/graphite_nginx_error.json":
    #         ensure  => file,
    #         replace => 'true',
    #         owner   => 'root',
    #         group   => 'root',
    #         mode    => '0644',
    #         content => template("sumo/graphite_nginx_error.json.erb");
    # }
}
